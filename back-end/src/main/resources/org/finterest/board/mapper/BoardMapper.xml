<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.finterest.invest.board.mapper.BoardMapper">

    <select id="getLatestRate" parameterType="String" resultType="org.finterest.invest.board.dto.BoardDTO">
        SELECT *
        FROM Conversion_Rates
        WHERE conversion_type = #{conversionType}
        ORDER BY created_at DESC
            LIMIT 1
    </select>

    <insert id="insertConversionRate" parameterType="org.finterest.invest.board.dto.BoardDTO">
        INSERT INTO Conversion_Rates (conversion_rate, created_at, updated_at)
        VALUES (#{conversionRate}, NOW(), NOW())
    </insert>

    <update id="updateConversionRate" parameterType="org.finterest.invest.board.dto.BoardDTO">
        UPDATE Conversion_Rates
        SET conversion_rate = #{conversionRate}, updated_at = NOW()
        WHERE rate_id = #{rateId}
    </update>

    <select id="getAllRates" resultType="org.finterest.invest.board.dto.BoardDTO">
        SELECT * FROM Conversion_Rates
    </select>

    <insert id="insertConversionTransaction" parameterType="org.finterest.invest.board.dto.BoardDTO">
        INSERT INTO Conversion_Transactions (user_id, money_change, point_change, conversion_type, conversion_date, rate_id)
        VALUES (#{userId}, #{moneyChange}, #{pointChange}, #{conversionType}, NOW(), #{rateId})
    </insert>

    <select id="getTransactionsByUserId" parameterType="int" resultType="org.finterest.invest.board.dto.BoardDTO">
        SELECT * FROM Conversion_Transactions
        WHERE user_id = #{userId}
    </select>



</mapper>
