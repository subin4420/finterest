<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.finterest.notice.mapper.NoticeMapper">
    <!-- 공지사항 목록 조회 -->
    <select id="getNotices" resultType="org.finterest.notice.domain.NoticeVO">
        SELECT
        n.notice_id AS noticeId,
        u.full_name AS writer,     <!-- Users 테이블에서 작성자 이름 가져오기 -->
        n.title AS title,
        n.content,
        n.created_at AS createdAt,
        n.updated_at AS updatedAt,
        n.view_count AS viewCount
        FROM
        Notices n
        LEFT JOIN Users u ON n.writer = u.user_id    <!-- Users 테이블과 조인 -->
        ORDER BY n.created_at DESC
    </select>

    <!-- 공지사항 상세 조회 -->
    <select id="getNoticeById" parameterType="int" resultType="org.finterest.notice.domain.NoticeVO">
        SELECT
        n.notice_id AS noticeId,
        u.full_name AS writer,     <!-- Users 테이블에서 작성자 이름 가져오기 -->
        n.title AS title,
        n.content,
        n.created_at AS createdAt,
        n.updated_at AS updatedAt,
        n.view_count AS viewCount
        FROM
        Notices n
        LEFT JOIN Users u ON n.writer = u.user_id    <!-- Users 테이블과 조인 -->
        WHERE
        n.notice_id = #{noticeId}
    </select>

    <!-- 공지사항 조회수 업데이트 (상세 조회 시 조회수 증가) -->
    <update id="incrementViewCount" parameterType="int">
        UPDATE Notices
        SET view_count = view_count + 1
        WHERE notice_id = #{noticeId}
    </update>
</mapper>